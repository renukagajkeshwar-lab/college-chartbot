POTE AI College Chatbot

Run on Python 3.8+: python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt; python app.py

Exact verified leadership/HOD answers are handled locally before Gemini. Gemini is optional and uses GEMINI_API_KEY/GEMINI_MODEL environment variables. Do not commit API keys.
